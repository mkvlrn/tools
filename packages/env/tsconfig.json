{
  "extends": ["../config/src/tsconfig-node.json"],
  "compilerOptions": {
    // no transpiling
    "noEmit": true,

    // paths
    "baseUrl": ".",
    "paths": {
      "#/*": ["./src/*"]
    }
  }
}
